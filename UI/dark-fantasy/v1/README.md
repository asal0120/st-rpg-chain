# MVU／XBX 正式 UI 素材

將整個 UI/dark-fantasy/v1/ 放在 asal0120/st-rpg-chain 倉庫相同路徑，分支 main。
本地已備妥 17 張 PNG，未代為上傳。兩個主檔已內含 CSS、網址及載入備援，不需另部署 JS。
assets.css 是網址對照用，不是另外必載的樣式檔。

正式預設使用 4 號收合箭頭；5 號保留為備選。其餘來源：9 號圓框、10 號分隔、12 號面板框、20 號按鈕。
所有 PNG 沿用已確認的透明裁片，沒有重新縮放；完整路徑與校驗值見 manifest.json。
素材未上傳或讀取失敗時，介面保留原控制項。不要預先在 HTML 寫入 data-stock-ready。
